# Быстрая инструкция по сборке

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Сборка для macOS
```bash
npm run build:mac
```
Результат: `release/GoRanked Chat Desk-1.0.0.dmg`

### 3. Сборка для Windows
```bash
npm run build:win
```
Результат: `release/GoRanked Chat Desk Setup 1.0.0.exe`

### 4. Сборка для Linux
```bash
npm run build:linux
```
Результат: `release/GoRanked Chat Desk-1.0.0.AppImage`

## 📋 Что происходит при сборке

1. **Компиляция TypeScript** → `dist/main.js`, `dist/preload.js`, `dist/renderer.js`
2. **Сборка React приложения** → `dist/index.html`, `dist/renderer.js`
3. **Упаковка в Electron** → `release/` с установщиками

## 🔧 Настройка перед сборкой

### Переменные окружения (опционально)

Создайте `.env` файл (не коммитится в git):

```env
API_URL=https://goranked.gg
WS_URL=wss://goranked.gg/app/osbq3zsjkpdjrtjkziqq
REVERB_HOST=goranked.gg
REVERB_PORT=443
REVERB_SCHEME=https
REVERB_APP_KEY=osbq3zsjkpdjrtjkziqq
```

По умолчанию используются значения из `webpack.config.js`.

## 📦 Структура после сборки

```
release/
├── GoRanked Chat Desk-1.0.0.dmg          # macOS Intel
├── GoRanked Chat Desk-1.0.0-arm64.dmg     # macOS Apple Silicon
├── GoRanked Chat Desk Setup 1.0.0.exe     # Windows
├── GoRanked Chat Desk-1.0.0.AppImage      # Linux
└── mac/                                    # Распакованное приложение (macOS)
```

## ⚠️ Важно

1. **Иконки**: Убедитесь, что файлы `assets/icon.ico` (Windows) и `assets/icon.icns` (macOS) существуют
2. **Версия**: Обновляйте версию в `package.json` перед каждой сборкой
3. **Тестирование**: Всегда тестируйте установщик на чистой системе перед раздачей пользователям

## 🐛 Решение проблем

### Ошибка "icon not found"
Создайте иконки:
- Windows: `assets/icon.ico` (256x256)
- macOS: `assets/icon.icns` (512x512)

Можно использовать онлайн конвертеры или Image2icon для macOS.

### Большой размер установщика
Это нормально для Electron приложений (обычно 100-200 MB). Electron включает Chromium и Node.js.

### Ошибки при сборке на другой платформе
- Для сборки Windows на macOS/Linux нужен Wine
- Для сборки macOS нужен Mac
- Для сборки Linux можно использовать Docker

