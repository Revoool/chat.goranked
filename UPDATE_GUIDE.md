# Руководство по обновлениям приложения

## 🔄 Как работают автообновления

Приложение автоматически проверяет наличие обновлений:
- При запуске приложения
- Каждые 4 часа во время работы

Если найдено обновление, пользователь получит уведомление и сможет установить его.

## 📦 Процесс выпуска обновления

### Шаг 1: Обновите версию

В `package.json` измените версию:

```json
{
  "version": "1.0.1"  // Было 1.0.0, стало 1.0.1
}
```

### Шаг 2: Соберите новую версию

```bash
# Для macOS
npm run build:mac

# Для Windows (нужен Windows или CI/CD)
npm run build:win
```

### Шаг 3: Загрузите файлы на сервер

Структура директории на сервере должна быть такой:

```
https://downloads.goranked.gg/chat-desk/
├── updates/
│   ├── darwin/
│   │   ├── latest-mac.yml          # Метаданные для macOS
│   │   ├── GoRanked Chat Desk-1.0.1-arm64.dmg
│   │   └── GoRanked Chat Desk-1.0.1.dmg
│   └── win32/
│       ├── latest.yml               # Метаданные для Windows
│       └── GoRanked Chat Desk Setup 1.0.1.exe
└── downloads/                       # Прямые ссылки для скачивания
    ├── GoRanked Chat Desk-1.0.1.dmg
    ├── GoRanked Chat Desk-1.0.1-arm64.dmg
    └── GoRanked Chat Desk Setup 1.0.1.exe
```

### Шаг 4: Создайте файлы метаданных

Electron-builder автоматически создает файлы `latest-mac.yml` и `latest.yml` в `release/`.

Скопируйте их в соответствующие директории на сервере:

```bash
# macOS
cp release/latest-mac.yml /var/www/downloads/chat-desk/updates/darwin/

# Windows
cp release/latest.yml /var/www/downloads/chat-desk/updates/win32/
```

## 🚀 Автоматизация через скрипт

Создайте скрипт `deploy-update.sh`:

```bash
#!/bin/bash

VERSION=$1
if [ -z "$VERSION" ]; then
  echo "Usage: ./deploy-update.sh 1.0.1"
  exit 1
fi

# Обновить версию в package.json (можно сделать вручную)
# npm version $VERSION

# Собрать приложение
echo "Building macOS..."
npm run build:mac

# Загрузить на сервер
echo "Uploading to server..."
scp release/*.dmg user@server:/var/www/downloads/chat-desk/downloads/
scp release/latest-mac.yml user@server:/var/www/downloads/chat-desk/updates/darwin/

echo "✅ Update $VERSION deployed!"
```

## 📋 Настройка Nginx для автообновлений

```nginx
server {
    listen 80;
    server_name downloads.goranked.gg;

    # Директория для обновлений
    location /chat-desk/updates/ {
        root /var/www/downloads;
        autoindex off;
        
        # CORS headers для electron-updater
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type";
        
        # Кеширование для метаданных
        location ~ \.(yml|yaml)$ {
            expires 1h;
        }
    }

    # Директория для прямых скачиваний
    location /chat-desk/downloads/ {
        root /var/www/downloads;
        autoindex on;
        
        # Разрешить скачивание больших файлов
        client_max_body_size 500M;
    }
}
```

## 🔧 Ручное обновление (если автообновления не работают)

Пользователи могут:
1. Скачать новую версию с вашего сайта
2. Установить поверх старой версии
3. Все данные сохранятся (токены в Keychain, настройки)

## ⚙️ Настройка URL для обновлений

По умолчанию используется: `https://downloads.goranked.gg/chat-desk/updates/`

Чтобы изменить, установите переменную окружения при сборке:

```bash
UPDATE_URL=https://your-server.com/updates/ npm run build:mac
```

Или измените в `src/main/main.ts`:

```typescript
autoUpdater.setFeedURL({
  provider: 'generic',
  url: 'https://your-server.com/updates/',
});
```

## 📝 Чеклист перед обновлением

- [ ] Обновлена версия в `package.json`
- [ ] Протестирована новая версия локально
- [ ] Собраны установщики для всех платформ
- [ ] Загружены файлы на сервер
- [ ] Загружены файлы метаданных (latest-mac.yml, latest.yml)
- [ ] Проверена доступность файлов по URL
- [ ] Протестировано автообновление на тестовой машине

## 🐛 Решение проблем

### Обновления не находятся

1. Проверьте доступность URL: `curl https://downloads.goranked.gg/chat-desk/updates/darwin/latest-mac.yml`
2. Проверьте версию в `latest-mac.yml` - она должна быть больше текущей
3. Проверьте логи приложения (DevTools → Console)

### Ошибка "Cannot find module 'electron-updater'"

Убедитесь, что пакет установлен:
```bash
npm install electron-updater --save-dev
```

### Пользователи не получают уведомления

- Проверьте, что приложение собрано в production режиме
- Проверьте, что файлы метаданных доступны
- Проверьте версию в package.json - она должна увеличиваться

## 💡 Альтернативные варианты обновлений

### Вариант 1: GitHub Releases

Если используете GitHub, можно настроить автообновления через GitHub Releases (требует токен).

### Вариант 2: S3/Cloud Storage

Можно использовать AWS S3, Google Cloud Storage и т.д. для хранения обновлений.

### Вариант 3: Собственный API

Создайте endpoint, который возвращает информацию об обновлениях:

```php
// Laravel пример
Route::get('/api/updates/check', function () {
    return response()->json([
        'version' => '1.0.1',
        'url' => 'https://downloads.goranked.gg/chat-desk/downloads/GoRanked-Chat-Desk-1.0.1.dmg',
        'releaseNotes' => 'Исправлены баги, улучшена производительность',
    ]);
});
```

