# Настройка Git и GitHub

## 🔑 Шаг 1: Создание SSH ключа

Если ключ уже создан, переходите к шагу 2.

```bash
# Создать новый SSH ключ
ssh-keygen -t ed25519 -C "goranked-chat-desk"

# Нажать Enter для сохранения в стандартное место
# Можно оставить пароль пустым или установить свой
```

## 📋 Шаг 2: Добавление ключа в GitHub

1. Скопируйте ваш публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   # или
   cat ~/.ssh/id_rsa.pub
   ```

2. Откройте https://github.com/settings/keys

3. Нажмите "New SSH key"

4. Вставьте скопированный ключ и сохраните

## ✅ Шаг 3: Проверка подключения

```bash
ssh -T git@github.com
```

Должно появиться: "Hi Revoool! You've successfully authenticated..."

## 🚀 Шаг 4: Настройка репозитория

```bash
cd chatapp

# Если git еще не инициализирован
git init
git add .
git commit -m "Initial commit"

# Настроить remote
git remote add origin git@github.com:Revoool/chat.goranked.git

# Запушить код
git branch -M main
git push -u origin main
```

## 📦 Шаг 5: Выпуск первой версии

```bash
# Обновить версию в package.json
# "version": "1.0.0"

# Создать тег
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0
git push origin main
git push origin v1.0.0
```

GitHub Actions автоматически соберет приложение и создаст релиз!

## 🔄 Обновление owner/repo в коде

После настройки репозитория обновите:

### package.json
```json
{
  "build": {
    "publish": [
      {
        "provider": "github",
        "owner": "Revoool",
        "repo": "chat.goranked"
      }
    ]
  }
}
```

### src/main/main.ts
```typescript
autoUpdater.setFeedURL({
  provider: 'github',
  owner: 'Revoool',
  repo: 'chat.goranked',
});
```

## 🐛 Решение проблем

### Ошибка "Permission denied (publickey)"

1. Проверьте, что ключ добавлен в GitHub
2. Проверьте подключение: `ssh -T git@github.com`
3. Убедитесь, что используете SSH URL (git@github.com), а не HTTPS

### Ошибка "Repository not found"

1. Проверьте, что репозиторий существует: https://github.com/Revoool/chat.goranked
2. Проверьте права доступа к репозиторию
3. Убедитесь, что используете правильный owner/repo

### Ключ не работает

Попробуйте добавить ключ в ssh-agent:
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
# или
ssh-add ~/.ssh/id_rsa
```

