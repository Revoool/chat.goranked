# Процесс обновления приложения - Пошаговая инструкция

## 🎯 Главное понимание

**Вам НЕ нужно каждый раз вручную раздавать файлы сотрудникам!**

После создания GitHub Release приложения сотрудников **автоматически** обнаружат обновление и предложат его установить.

---

## 📋 Процесс обновления (пошагово)

### Шаг 1: Внесли изменения в код ✅
Вы уже сделали изменения и запушили в git.

### Шаг 2: Обновили версию ✅
Версия в `package.json` уже обновлена до `1.0.2`.

### Шаг 3: Создать релиз на GitHub

**Вариант А: Автоматический (через GitHub Actions)**

1. Создайте и запушьте git tag:
   ```bash
   git tag v1.0.2
   git push origin v1.0.2
   ```

2. GitHub Actions автоматически:
   - Соберет приложение для Windows
   - Создаст GitHub Release
   - Загрузит установщик и файлы для автообновления

3. Готово! Приложения сотрудников автоматически получат обновление.

**Вариант Б: Ручной (если GitHub Actions не работает)**

1. Соберите приложение локально:
   ```bash
   npm run build:win
   ```

2. Перейдите на GitHub: https://github.com/Revoool/chat.goranked/releases

3. Нажмите "Create a new release"

4. Заполните:
   - **Tag version:** `v1.0.2` (создайте новый tag)
   - **Release title:** `GoRanked Chat Desk v1.0.2`
   - **Description:** Опишите изменения

5. Загрузите файлы из папки `release/`:
   - `GoRanked Chat Desk-Setup-1.0.2.exe` (установщик)
   - `latest.yml` (обязательно! для автообновления)

6. Нажмите "Publish release"

### Шаг 4: Автоматическое обновление у сотрудников

После публикации релиза:
- ✅ Приложения сотрудников автоматически обнаружат обновление
- ✅ Обновление скачается в фоне
- ✅ Пользователь получит уведомление
- ✅ Приложение предложит перезапуститься для установки

**Время обнаружения:** до 30 минут (или при следующем запуске приложения)

---

## 🔄 Что происходит дальше?

1. **Сотрудники НЕ получают файлы вручную** - всё автоматически
2. **Приложение само проверяет обновления** каждые 30 минут
3. **При обнаружении новой версии** показывается уведомление
4. **Пользователь подтверждает** и приложение обновляется

---

## 📝 Пример для следующего обновления

Когда захотите выпустить версию 1.0.3:

1. Внесите изменения в код
2. Обновите версию в `package.json`: `"version": "1.0.3"`
3. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Update to v1.0.3"
   git push
   ```
4. Создайте tag и релиз:
   ```bash
   git tag v1.0.3
   git push origin v1.0.3
   ```
5. GitHub Actions соберет и опубликует автоматически
6. Сотрудники получат обновление автоматически

---

## ❓ Частые вопросы

**Q: Нужно ли каждый раз собирать `npm run build:win`?**
A: Нет! Если используете GitHub Actions, он соберет автоматически. Если вручную - да, но только для загрузки в GitHub Release.

**Q: Нужно ли раздавать файлы сотрудникам?**
A: Нет! После публикации релиза приложения автоматически обнаружат обновление.

**Q: Как быстро сотрудники получат обновление?**
A: В течение 30 минут (или при следующем запуске приложения).

**Q: Что если GitHub Actions не работает?**
A: Используйте ручной способ - соберите локально и загрузите файлы в GitHub Release.

