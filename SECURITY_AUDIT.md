# Отчет по безопасности приложения chatapp

## Дата анализа: 2025-12-26

## Обзор
Приложение является Electron-приложением для работы с чатами менеджеров и клиентов. Код находится в открытом доступе на GitHub, что требует особого внимания к безопасности.

---

## ✅ Положительные моменты

1. **Хранение токенов**: Токены хранятся в системном keychain (keytar) для macOS/Windows, что является безопасным подходом
2. **Авторизация API**: Все API запросы требуют токен в заголовке `Authorization: Bearer {token}`
3. **Обработка ошибок 401**: При истечении токена происходит автоматический logout
4. **Gitignore**: `.env` файлы правильно исключены из репозитория
5. **WebSocket авторизация**: WebSocket подключение требует токен для подписки на каналы

---

## ⚠️ Найденные проблемы безопасности

### 🔴 КРИТИЧЕСКИЕ

#### 1. **REVERB_APP_KEY может быть пустым в production**
**Файл**: `webpack.config.js:54-61`
```javascript
'REVERB_APP_KEY': JSON.stringify(process.env.REVERB_APP_KEY || (() => {
  if (isProduction) {
    throw new Error('REVERB_APP_KEY environment variable is required for production builds');
  }
  // Only allow fallback in development mode
  console.warn('⚠️  REVERB_APP_KEY not set, using empty string. Set it in .env file!');
  return '';
})()),
```

**Проблема**: Если `REVERB_APP_KEY` не установлен, используется пустая строка, что может позволить подключиться к WebSocket без авторизации.

**Рекомендация**: 
- Убедиться, что `REVERB_APP_KEY` всегда устанавливается при сборке production
- Добавить проверку на сервере, что WebSocket подключения требуют валидный токен

#### 2. **Mock режим может быть включен в production**
**Файл**: `src/renderer/api/client.ts:57-70`
```javascript
const MOCK_MODE = process.env.MOCK_MODE === 'true';

if (MOCK_MODE && (email === 'test@goranked.gg' || email === 'demo@test.com')) {
  // Mock response for testing
  return {
    accessToken: 'mock-token-' + Date.now(),
    ...
  };
}
```

**Проблема**: Если `MOCK_MODE=true` установлен в production, можно войти без реальных учетных данных.

**Рекомендация**:
- Удалить mock режим из production сборки или добавить проверку `isProduction`
- Использовать только в development режиме

#### 3. **Прямые ссылки на файлы без проверки доступа**
**Файл**: `src/renderer/components/MessageItem.tsx:249-251`
```tsx
<img src={file.file_path} alt={file.file_name} />
<a href={file.file_path} target="_blank" rel="noopener noreferrer">
```

**Проблема**: Если `file_path` содержит прямую ссылку на файл, злоумышленник может получить доступ к файлам, зная путь.

**Рекомендация**:
- Использовать защищенные эндпоинты для загрузки файлов: `/api/manager-client-chats/{chatId}/messages/{messageId}/files/{fileId}`
- На сервере проверять, что пользователь имеет доступ к чату перед выдачей файла
- Использовать временные токены для доступа к файлам

### 🟡 СРЕДНИЕ

#### 4. **WebSocket подписка на публичные каналы**
**Файл**: `src/renderer/api/websocket.ts:172-184`
```typescript
private subscribeToChannels() {
  // Subscribe to manager-client-chats channel (public channel for all events)
  if (this.ws && this.ws.readyState === WebSocket.OPEN) {
    const subscribeMessage = JSON.stringify({
      event: 'pusher:subscribe',
      data: {
        channel: 'manager-client-chats',
      },
    });
```

**Проблема**: Подписка на публичный канал `manager-client-chats` может позволить получать события для всех чатов.

**Рекомендация**:
- Убедиться, что на сервере (Laravel Reverb) есть проверка авторизации перед подпиской
- Использовать приватные каналы с авторизацией
- Фильтровать события на сервере по правам доступа пользователя

#### 5. **Логирование токенов и чувствительных данных**
**Файл**: `src/renderer/api/client.ts:49, 78`
```typescript
console.log('Token received:', token ? 'Yes' : 'No');
console.log('Login response:', response.data);
```

**Проблема**: В production сборке console.log может выводить чувствительные данные в DevTools.

**Рекомендация**:
- Удалить или заменить на условное логирование только в development режиме
- Использовать webpack для удаления console.log в production

#### 6. **Fallback на localStorage для токенов**
**Файл**: `src/renderer/api/client.ts:44, 59, 66`
```typescript
return localStorage.getItem('token');
localStorage.setItem('token', token);
```

**Проблема**: Если keytar не работает, токен сохраняется в localStorage, что менее безопасно.

**Рекомендация**:
- Это приемлемый fallback, но нужно убедиться, что keytar всегда работает в production
- Добавить предупреждение пользователю, если используется localStorage

### 🟢 НИЗКИЕ

#### 7. **Отсутствие проверки CORS на клиенте**
**Проблема**: Electron приложение не имеет встроенной защиты от CORS, но это не критично, так как запросы идут напрямую к API.

**Рекомендация**: Убедиться, что на сервере правильно настроен CORS для API.

#### 8. **Отсутствие проверки целостности обновлений**
**Файл**: `src/main/main.ts:270`
```typescript
autoUpdater.setFeedURL(feedURLConfig);
```

**Проблема**: Нет явной проверки подписи обновлений.

**Рекомендация**: 
- Использовать code signing для приложения
- Проверять SHA512 хеши обновлений (уже есть в electron-updater)

---

## 📋 Рекомендации по улучшению безопасности

### Немедленные действия (критично)

1. **Удалить mock режим из production**:
   ```typescript
   // В webpack.config.js добавить проверку
   'process.env.MOCK_MODE': JSON.stringify(
     isProduction ? 'false' : (process.env.MOCK_MODE || 'false')
   ),
   ```

2. **Использовать защищенные эндпоинты для файлов**:
   - Изменить `file.file_path` на использование API эндпоинта
   - Пример: `/api/manager-client-chats/{chatId}/messages/{messageId}/files/{fileId}`

3. **Проверить авторизацию WebSocket на сервере**:
   - Убедиться, что Laravel Reverb проверяет токен перед подпиской
   - Использовать приватные каналы вместо публичных

### Среднесрочные улучшения

4. **Добавить проверку окружения**:
   ```typescript
   if (process.env.NODE_ENV === 'production' && process.env.MOCK_MODE === 'true') {
     throw new Error('MOCK_MODE cannot be enabled in production');
   }
   ```

5. **Удалить console.log из production**:
   ```javascript
   // В webpack.config.js
   new webpack.DefinePlugin({
     'process.env.NODE_ENV': JSON.stringify(argv.mode || 'development'),
   }),
   // Использовать terser-webpack-plugin для удаления console.log
   ```

6. **Добавить rate limiting на клиенте**:
   - Ограничить количество запросов к API
   - Добавить задержки при ошибках

### Долгосрочные улучшения

7. **Code signing для приложения**:
   - Подписать приложение для macOS и Windows
   - Это повысит доверие и безопасность обновлений

8. **Добавить проверку целостности**:
   - Проверять хеши файлов перед использованием
   - Валидировать сертификаты при обновлениях

9. **Аудит зависимостей**:
   - Регулярно проверять уязвимости в `node_modules`
   - Использовать `npm audit` и обновлять зависимости

---

## 🔒 Проверка на сервере (Laravel)

**ВАЖНО**: Безопасность также зависит от серверной части. Убедитесь, что:

1. ✅ Все API эндпоинты требуют авторизацию (`auth:sanctum` или аналогичное)
2. ✅ Файлы доступны только авторизованным пользователям с правами на чат
3. ✅ WebSocket каналы требуют авторизацию перед подпиской
4. ✅ Проверяется, что пользователь имеет доступ к конкретному чату
5. ✅ Rate limiting настроен для API запросов
6. ✅ CORS настроен правильно (только для доверенных доменов)

---

## ✅ Заключение

Приложение имеет хорошую базовую безопасность:
- ✅ Токены хранятся безопасно (keytar)
- ✅ Все API запросы требуют авторизацию
- ✅ Нет хардкодных секретов в коде

**Основные риски**:
- ⚠️ Mock режим может быть включен в production
- ⚠️ Прямые ссылки на файлы без проверки доступа
- ⚠️ WebSocket может не проверять авторизацию на сервере

**Приоритет исправлений**: Критичные проблемы должны быть исправлены перед следующим релизом.

---

## 📝 Чеклист перед релизом

- [ ] Убедиться, что `MOCK_MODE` не может быть включен в production
- [ ] Проверить, что `REVERB_APP_KEY` всегда установлен при сборке
- [ ] Изменить загрузку файлов на защищенные эндпоинты
- [ ] Проверить авторизацию WebSocket на сервере
- [ ] Удалить или условно включить console.log в production
- [ ] Провести аудит зависимостей (`npm audit`)
- [ ] Проверить, что на сервере все эндпоинты защищены
- [ ] Убедиться, что файлы доступны только авторизованным пользователям

