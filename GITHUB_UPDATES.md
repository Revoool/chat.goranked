# Автообновления через GitHub Releases

## 🚀 Быстрый старт

### 1. Создайте репозиторий на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/goranked-chat-desk.git
git push -u origin main
```

### 2. Обновите настройки в коде

В `package.json` укажите ваш GitHub username и название репозитория:

```json
{
  "build": {
    "publish": [
      {
        "provider": "github",
        "owner": "your-username",      // ← Ваш GitHub username
        "repo": "goranked-chat-desk"   // ← Название репозитория
      }
    ]
  }
}
```

В `src/main/main.ts` также обновите (или используйте переменные окружения):

```typescript
autoUpdater.setFeedURL({
  provider: 'github',
  owner: 'your-username',        // ← Ваш GitHub username
  repo: 'goranked-chat-desk',    // ← Название репозитория
});
```

### 3. Создайте GitHub Token

1. Зайдите в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создайте токен с правами `repo`
3. Сохраните токен (он понадобится для сборки)

### 4. Настройте GitHub Actions

Файл `.github/workflows/build.yml` уже создан. Он автоматически:
- Соберет приложение при создании тега
- Создаст релиз на GitHub
- Загрузит установщики

### 5. Выпустите первую версию

```bash
# Обновите версию в package.json
# "version": "1.0.0"

# Создайте тег
git tag v1.0.0
git push origin v1.0.0
```

GitHub Actions автоматически соберет и создаст релиз!

## 🔄 Как выпускать обновления

### Шаг 1: Обновите версию

В `package.json`:
```json
{
  "version": "1.0.1"  // Увеличьте версию
}
```

### Шаг 2: Создайте тег и запушьте

```bash
git add .
git commit -m "Release v1.0.1"
git tag v1.0.1
git push origin main
git push origin v1.0.1
```

### Шаг 3: GitHub Actions соберет автоматически

Через несколько минут релиз будет готов, и пользователи получат уведомление об обновлении!

## ⚙️ Режимы обновлений

### Обычный режим (по умолчанию)

Пользователь получает уведомление и может выбрать:
- **Перезапустить** - установить обновление сейчас
- **Позже** - отложить обновление

### Принудительное обновление

Чтобы сделать обновление обязательным, в `src/main/main.ts` раскомментируйте:

```typescript
autoUpdater.on('update-downloaded', (info) => {
  console.log('✅ Update downloaded:', info.version);
  
  // ПРИНУДИТЕЛЬНОЕ ОБНОВЛЕНИЕ
  autoUpdater.quitAndInstall(false, true);
  return;
  
  // Остальной код не выполнится
});
```

⚠️ **Внимание**: Принудительное обновление может раздражать пользователей, используйте осторожно!

### Умное принудительное обновление

Можно сделать принудительным только для критических обновлений:

```typescript
autoUpdater.on('update-downloaded', (info) => {
  // Проверяем, критическое ли обновление (например, по версии)
  const isCritical = info.version.startsWith('1.0.') && parseInt(info.version.split('.')[2]) >= 5;
  
  if (isCritical) {
    // Критическое обновление - принудительно
    dialog.showMessageBoxSync(mainWindow, {
      type: 'warning',
      title: 'Критическое обновление',
      message: 'Требуется обязательное обновление для безопасности. Приложение будет перезапущено.',
      buttons: ['ОК'],
    });
    autoUpdater.quitAndInstall(false, true);
  } else {
    // Обычное обновление - с выбором
    // ... обычный код
  }
});
```

## 🔐 Настройка токена для GitHub Actions

Для автоматической сборки нужен GitHub Token:

1. Создайте токен: GitHub → Settings → Developer settings → Personal access tokens
2. Добавьте в Secrets репозитория:
   - Settings → Secrets and variables → Actions → New repository secret
   - Name: `GH_TOKEN`
   - Value: ваш токен

3. Обновите `.github/workflows/build.yml`:

```yaml
- name: Create Release
  uses: softprops/action-gh-release@v1
  with:
    files: release/*.dmg
  env:
    GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}  # Используйте ваш секрет
```

## 📋 Структура релиза на GitHub

После создания релиза на GitHub будет:

```
v1.0.1 Release
├── GoRanked Chat Desk-1.0.1.dmg          (macOS Intel)
├── GoRanked Chat Desk-1.0.1-arm64.dmg   (macOS Apple Silicon)
└── GoRanked Chat Desk Setup 1.0.1.exe   (Windows)
```

Electron-updater автоматически найдет правильный файл для каждой платформы.

## 🐛 Решение проблем

### Обновления не находятся

1. Проверьте, что релиз создан на GitHub
2. Проверьте версию в `package.json` - она должна быть меньше новой версии
3. Проверьте права токена (нужен `repo`)
4. Проверьте логи приложения (DevTools → Console)

### Ошибка "Cannot find release"

- Убедитесь, что релиз публичный (не draft)
- Проверьте правильность owner/repo в настройках
- Убедитесь, что файлы загружены в релиз

### GitHub Actions не работает

- Проверьте, что файл `.github/workflows/build.yml` существует
- Проверьте, что тег создан правильно (`v1.0.0`)
- Проверьте логи в Actions → Workflow runs

## 💡 Преимущества GitHub Releases

✅ Автоматическая сборка через GitHub Actions  
✅ Не нужен отдельный сервер для обновлений  
✅ Версионирование через теги  
✅ История изменений в релизах  
✅ Бесплатно для публичных репозиториев  

## 🔄 Альтернатива: Собственный сервер

Если хотите использовать свой сервер вместо GitHub, установите переменную окружения:

```bash
UPDATE_URL=https://downloads.goranked.gg/chat-desk/updates/ npm run build:mac
```

Или измените в `src/main/main.ts` на `provider: 'generic'`.

